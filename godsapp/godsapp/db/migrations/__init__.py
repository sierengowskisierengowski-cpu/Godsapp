"""Migrations live here as numbered Python modules with `upgrade(conn)`.

For 0.1.0 we rely on `Base.metadata.create_all` (init_db). When schema changes
land, add 0002_*.py modules and have a `migrate()` runner walk them.
"""
